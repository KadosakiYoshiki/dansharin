services:
  web:
    build: .
    environment:
      TZ: ${TZ}
    command: bash -c "rm -f tmp/pids/server.pid && bundle check || bundle install && rails s -p 3000 -b '0.0.0.0'"
    volumes:
      - .:/docker_app
    ports:
      - 3000:3000
    stdin_open: true
    tty: true
